.git
.gitignore
.dockerignore
Dockerfile
docker-compose.yml

tmp/
log/
storage/
node_modules/

# Secrets must never be baked into an image; production supplies
# RAILS_MASTER_KEY etc. via environment variables (.env.production).
.env*
config/master.key
config/credentials/*.key
*.md

# Local dev artifacts
.DS_Store
coverage/
